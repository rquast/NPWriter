import NPWOverlayMenu from './NPWOverlayMenu'

export default {
    name: 'npw-overlay-menu',
    configure: function(config) {
        config.addComponent('npw-overlay-menu', NPWOverlayMenu)
    }
}
