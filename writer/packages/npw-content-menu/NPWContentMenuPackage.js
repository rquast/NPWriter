import NPWContentMenu from './NPWContentMenu'

export default {
    name: 'npw-content-menu',
    configure: function(config) {
        config.addComponent('npw-content-menu', NPWContentMenu)
    }
}
