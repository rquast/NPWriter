import { Component } from 'substance'

class SidebarPanelComponent extends Component {

    render($$) {
        return $$('div').addClass('se-context-section').append(
          'TODO: Render the actual sidebar panel'
        )
    }
}

export default SidebarPanelComponent
